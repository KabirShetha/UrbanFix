import React from 'react';
import { View, Image } from 'react-native';

const IconlyLightProfile = () => {
  return (
    <View>
      <Image source={require('./path/to/iconly-light-profile.png')} />
    </View>
  );
};

export default IconlyLightProfile;